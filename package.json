{
  "name": "client-platform-kernel",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @client-platform/kernel && npm run build -w @client-platform/cli",
    "client-platform": "node packages/cli/bin/client-platform.js"
  }
}
